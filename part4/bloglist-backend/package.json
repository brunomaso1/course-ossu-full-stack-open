{
  "name": "bloglist-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bloglist:start": "cross-env NODE_ENV=production node index.js",
    "bloglist:dev": "cross-env NODE_ENV=development nodemon index.js",
    "bloglist:build:ui": "rm -rf build && cd ../frontend/ && npm run build && cp -r build ../backend",
    "bloglist:deploy": "fly deploy",
    "bloglist:deploy:full": "npm run build:ui && npm run deploy",
    "bloglist:logs:prod": "fly logs",
    "bloglist:lint": "eslint .",
    "bloglist:test": "cross-env NODE_ENV=test jest --verbose --runInBand"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.52.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "lodash": "^4.17.21",
    "mongoose": "^7.6.3",
    "mongoose-unique-validator": "^4.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "globalTeardown": "./teardown.js"
  }
}
