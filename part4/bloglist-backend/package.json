{
  "name": "bloglist-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bloglist:start": "node index.js",
    "bloglist:dev": "nodemon index.js",
    "bloglist:build:ui": "rm -rf build && cd ../frontend/ && npm run build && cp -r build ../backend",
    "bloglist:deploy": "fly deploy",
    "bloglist:deploy:full": "npm run build:ui && npm run deploy",
    "bloglist:logs:prod": "fly logs",
    "bloglist:lint": "eslint .",
    "bloglist:test": "jest --verbose"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "lodash": "^4.17.21",
    "mongoose": "^7.6.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
